_dev-run COMMAND:
  nix develop .# --command sh -c "{{COMMAND}}"

blog-build:
  just _dev-run "zola build"

blog-watch:
  just _dev-run "zola serve"

blog-edit:
  zellij action new-tab --layout .zellij.kdl
